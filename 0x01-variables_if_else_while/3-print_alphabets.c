#include <stdio.h>
/**
 * main - alphabet in both upper and lower
 * Return: success 0
 */
	int main(void)
{	
	char c;
	c = 'a' == 'A';
	while
		(c <= 'z' && 'Z') {
			putchar(c);
			c++;
		}
	putchar('\n');

	return (0);
}


